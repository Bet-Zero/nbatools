# Core Result/Table Contracts

This reference locks the current Wave 1 and Wave 2 route -> result shape -> table pattern
contracts used by the React result renderer. It documents current behavior only:
no visual redesign, no route-registry redesign, and no new query capability.

Source files:

- `frontend/src/components/results/config/routeToPattern.ts`
- `frontend/src/components/results/resultShapes.ts`
- `frontend/src/components/results/patterns/*Result.tsx`
- `src/nbatools/commands/structured_results.py`

## Shared Table Behavior

| Pattern family | Current visible-column behavior | Hidden/detail behavior | Row cap | Footer/total behavior | Highlight behavior |
|---|---|---|---|---|---|
| Entity Summary | Hero sentence from `summary[0]`; optional `by_season` table for multi-period scopes with `Season`, `GP`, optional `W-L`, and available average/rate columns. | No raw detail drawer. | Hero row is single-row; by-season has no frontend cap. | None. | None. |
| Player Game Log | `#`, optional `Player`, `Date`, `TM`, location marker, `Opp`, optional `Score`, optional `W/L`, then available `MIN`, `PTS`, `REB`, `AST`, `FG`, `3P`, `FT`, `STL`, `BLK`, `TOV`, `+/-`, `TS%`, `eFG%`. | Finder routes show a raw-detail toggle when rows contain additional fields. Player summary game logs do not set a raw-detail title. | Product mode initially shows 12 rows with a "Show all" toggle; review mode has no cap. Backend finder routes may also limit rows. | If a `summary` row is supplied to `GameLogResult`, `Average` and `Total` footer rows render for supported stat columns. | Metric columns from route metadata/stat filters are highlighted when present. |
| Team Game Log | `#`, `Date`, `Team`, location marker, `Opp`, optional `Score`, optional `W/L`, then available `PTS`, `Opp PTS`, `Margin`, `REB`, `AST`, `3PM`, `FG`, `3P`, `FT`, `TOV`, `STL`, `BLK`, `ORB`, `DRB`. | Finder, game summary, and stacked team-record game logs can show raw/detail toggles when configured and additional fields exist. | Product mode initially shows 12 rows with a "Show all" toggle; review mode has no cap. Backend finder routes may also limit rows. | If a `summary` row is supplied, `Average` and `Total` footer rows render for supported stat columns. | Metric columns from route metadata/stat filters are highlighted when present. |
| Team Record | Summary table: `Team`, `W-L`, available `Games`, `Win %`, `PPG`, `+/-`, `REB`, `AST`, `3PM`, context columns (`Season Type`, `Opponent Group`, `Season`, `Home/Away`, `Opponent`), then available optional stats (`Opp PPG`, `Net`, `STL`, `BLK`, `TOV`, `eFG%`, `TS%`). Optional by-season table adds `Season`, `Games`, `W-L`, and available season stats. | Raw-detail toggles appear when summary or by-season rows include additional fields beyond displayed columns. | No frontend cap for record tables. Optional stacked game log uses the game-log 12-row product cap. | None. | Summary record column is highlighted; by-season highlights `win_pct`. |
| Leaderboard | `#`, entity label (`Player`, `Team`, or `Name`), primary metric, then dynamically selected supporting columns from row keys. Current behavior -- needs product decision: supporting columns are inferred, not route-declared. | No raw detail drawer. | No frontend cap. Backend limit is route-specific, usually 10. | None. | Primary metric column is highlighted. |
| Top Performances | `Rank`, `Player` or `Team`, `Date`, `Opp`, `Result`, optional `Score`, primary metric, then supporting stats. For points, supporting stats prefer `REB`, `AST`, `3PM` when present. | No raw detail drawer. | No frontend cap. Backend limit is route-specific, usually 10. | Optional note: "Showing top N of total" when result metadata exposes a larger total. | Primary metric column is highlighted. |
| Rolling Stretch | League/named-window table: `Rank`, `Player`, `Window`, primary stretch metric, `Start`, `End`, `Season`, plus up to two supporting window columns. Named-player payloads can also render `Best Window Games` with `Date`, `Opp`, `Result`, and available game stats. | No raw detail drawer. Named-player mode can show a second game-log table from `best_window_game_log`, `window_game_log`, or `game_log`. | No frontend cap. Backend limit is route-specific, usually 10. | Optional note: "Showing top N of total" when result metadata exposes a larger total. | `stretch_value` is highlighted; named-player best-window game log highlights the requested metric when present. |
| Split Comparison | `Split`, `GP`, optional `Record`, then available split metrics in this order: `Poss`, `ORtg`, `DRtg`, `Net`, `Pace`, `PTS`, `REB`, `AST`, `MIN`, `3PM`, `TS%`, `eFG%`, `3P%`, `+/-`. | `Split Summary Detail` renders from `summary` when separate from the primary section. `Split Comparison Detail` renders from the primary rows. | No frontend cap. | None. | Edge chips render for two-bucket splits when a supported metric differs. |
| On/Off Split | Same split table primitive, but primary rows come from `summary`, bucket labels use `presence_state`, and the split label is forced to `On/Off`. Common columns: `Split`, `GP`, `ORtg`, `DRtg`, `Net`, `MIN`, `+/-`. | `On/Off Detail` renders from the same `summary` rows. No separate summary detail. Current behavior -- needs product decision: backend uses `SummaryResult.summary`, not `SplitSummaryResult.split_comparison`. | No frontend cap. | None. | Edge chips render when both on/off rows are present and supported metrics differ. |
| Streak Table | `#`, optional `Player`/`Team`, `Streak`, `Length`, conditional `Status`, `Start`, `End`, `Games`, optional `Record`, then available average columns: `PTS`, `REB`, `AST`, `MIN`, `TS%`, `eFG%`, `3PM`, `+/-`. | `Full Streak Detail` renders only when rows contain undisplayed fields. | No frontend cap. Backend default limit is 25. | None. | None. `Status` appears for active streaks or mixed active/completed rows; a single completed-only sample does not show `Status`. |
| Comparison Metrics | Subject cards from `summary`; metric table with `Metric`, one value column per compared entity, and `Edge / Difference`. Primary metrics are ordered before secondary metrics. | `Player Summary Detail`, `Team Summary Detail`, or `Participant Detail` for extra `summary` fields; `Full Metric Detail` or `Metric Detail` for extra `comparison` fields; other populated sections get generic detail toggles. | Primary metric table initially shows up to 12 preferred metrics, with a `Show more metrics` toggle when secondary metrics exist. | None. | Edge/difference column is derived from metric direction and value delta, or from `__edge_label` when supplied. |
| Playoff History | History mode: `Season`, `Round Reached`, `Record`, optional `Result`, optional `Opponent`, optional `Win Pct`, optional `Games`. Round-record mode: `#`, `Team`, `Round`, `Record`, optional target metric, `Games`, `Win Pct`, `Seasons`, `Series`. Matchup mode: `Season`, `Round`, `Winner`, `Series Result`, one record column per team abbreviation, optional `Games`. | History/matchup modes expose postseason summary and row detail toggles when additional fields exist. Round-record mode exposes `Playoff Round Detail` when rows contain undisplayed fields. | No frontend cap. Backend limits leaderboard-style round records. | None. | Round-record mode highlights the selected metric (`win_pct`, `wins`, `games_played`, `games`, or `series`). |
| Record By Decade | `Decade`, optional `Seasons`, `W-L`, `Win %`, optional `Games`, optional `Season Type`. | `Record Detail` renders from the one-row `summary`. | No frontend cap. | None. | `Win %` is highlighted. |
| Record By Decade Leaderboard | `#`, `Team`, optional `Decade`, primary metric (`Wins`, `Losses`, `Win %`, `Games`, etc.), `W-L`, optional `Games`, optional `Seasons`, optional `Season Type`. | No raw detail drawer. | No frontend cap. Backend limit is route-specific, usually top N per decade. | None. | Selected metric is highlighted. |
| Matchup By Decade | `Decade`, `Games`, one `W-L` column per team prefix, optional per-team `Win %`, optional per-team `PPG`. | `Matchup Summary Detail` renders from `summary`. | No frontend cap. | None. | None. |
| Lineup Summary | Entity-summary hero sentence from `summary[0]`; lineup labels come from `lineup`, `lineup_members`, or `members`. Current hero sentence surfaces `net_rating`, `off_rating`, `def_rating`, and `pace` when present. | No raw detail drawer and no summary table. Current behavior -- needs product decision if lineup summaries need a richer dedicated pattern. | Hero row is single-row. | None. | None. |

## Wave 1 Route Contracts

| Route | PatternConfig | Result shape | Required sections | Optional sections | Table family / mode | Expected row type | Examples |
|---|---|---|---|---|---|---|---|
| `player_game_summary` | Default: `{ type: "entity_summary", sectionKey: "summary" }`. With qualifying game rows: add `{ type: "game_log", sectionKey: "game_log", summaryKey: "summary", showSummaryStrip: false }`. | `entity_summary` or `entity_summary_with_gamelog`. | `summary` | `by_season`, `game_log` | Entity Summary; optional Player Game Log. | `summary`: one player aggregate row. `game_log`: player game row. | "Curry this season"; "Jokic last 10 games"; "Tatum against good teams" |
| `player_game_finder` | `{ type: "game_log", sectionKey: "finder", mode: "player", rawDetailTitle: "Player Game Detail" }` | `game_log_player_table` | `finder` | None currently. | Player Game Log. | Player game row matching threshold/filter. | "Curry 5+ threes"; "Jokic 20+ rebounds" |
| `game_finder` | `{ type: "game_log", sectionKey: "finder", mode: "team", rawDetailTitle: "Game Detail" }` | `game_log_team_table` | `finder` | None currently. | Team Game Log. | Team game row matching threshold/filter. | "Lakers 120+ points"; "Celtics allowed under 100" |
| `game_summary` | `{ type: "game_log", sectionKey: "game_log", summaryKey: "summary", mode: "team", rawDetailTitle: "Game Detail", detailSectionKeys: ["top_performers"] }` | `game_log_team_detail` | `game_log` | `summary`, `by_season`, `top_performers` | Game Summary Log, team mode. | `game_log`: team game row. `top_performers`: player performance detail row. | "Celtics last 5 games"; "Lakers this season" |
| `team_record` | Default: `{ type: "record", mode: "team_record" }`. With `game_log` rows: add `{ type: "game_log", sectionKey: "game_log", summaryKey: "summary", mode: "team", showSummaryStrip: false, rawDetailTitle: "Game Detail" }`. | `team_record` | `summary` | `by_season`, `game_log` | Team Record; optional stacked Team Game Log. | `summary`: one team record aggregate row. `game_log`: team game row, currently produced for without-player filters and similar supported record contexts. | "Sixers record vs .500 teams"; "Knicks record without Brunson" |
| `season_leaders` | `{ type: "leaderboard", sectionKey: "leaderboard" }` | `leaderboard_table` | `leaderboard` | None currently. | Leaderboard. | Ranked player season aggregate row. | "Top scorers this season"; "Most rebounds in 2025 playoffs" |
| `season_team_leaders` | `{ type: "leaderboard", sectionKey: "leaderboard" }` | `leaderboard_table` | `leaderboard` | None currently. | Leaderboard. | Ranked team season aggregate row. | "Team assists leaders"; "Most wins by a team" |
| `top_player_games` | `{ type: "top_performances", sectionKey: "leaderboard", subject: "player" }` | `top_performances` | `leaderboard` | None currently. | Top Performances, player subject. | Ranked single-player game row. | "Highest scoring games this season"; "Most threes in a game" |
| `top_team_games` | `{ type: "top_performances", sectionKey: "leaderboard", subject: "team" }` | `top_performances` | `leaderboard` | None currently. | Top Performances, team subject. | Ranked single-team game row. | "Highest team scoring games"; "Most team points this season" |
| `player_occurrence_leaders` | `{ type: "leaderboard", sectionKey: "leaderboard" }` | `leaderboard_table` | `leaderboard` | None currently. | Leaderboard. | Ranked player occurrence-count row. | "Most 40-point games"; "Players with the most 10-assist games" |
| `team_occurrence_leaders` | `{ type: "leaderboard", sectionKey: "leaderboard" }` | `leaderboard_table` | `leaderboard` | None currently. | Leaderboard. | Ranked team occurrence-count row. | "Teams with the most 120-point games"; "Most games allowing under 100" |
| `player_stretch_leaderboard` | `{ type: "rolling_stretch", sectionKey: "leaderboard" }` | `rolling_stretch` | `leaderboard` | `best_window_game_log`, `window_game_log`, `game_log` for named-player detail when present. | Rolling Stretch. | Ranked rolling-window row; optional named-player game row detail. | "Best 10-game scoring stretch"; "LeBron best 10-game stretch" |

## Wave 2 Route Contracts

| Route | PatternConfig | Result shape | Required sections | Optional sections | Table family / mode | Expected row type | Current visible columns and behavior | Examples |
|---|---|---|---|---|---|---|---|---|
| `player_split_summary` | `{ type: "split", subject: "player" }` | `split_table` | `summary`, `split_comparison` | None currently. | Split Comparison, player subject. | `summary`: one split context row. `split_comparison`: one row per bucket. | `Split`, `GP`, optional `Record`, available split metrics (`Poss`, `ORtg`, `DRtg`, `Net`, `Pace`, `PTS`, `REB`, `AST`, `MIN`, `3PM`, `TS%`, `eFG%`, `3P%`, `+/-`). Shows summary and split detail toggles; edge chips for two-bucket splits; no row cap/footer/highlight. | "Curry home away splits"; "LeBron splits in wins and losses" |
| `team_split_summary` | `{ type: "split", subject: "team" }` | `split_table` | `summary`, `split_comparison` | None currently. | Split Comparison, team subject. | `summary`: one team split context row. `split_comparison`: one row per bucket. | Same Split Comparison columns and behavior as player splits, driven by available row keys. | "Suns splits by location"; "Celtics wins losses split" |
| `player_on_off` | `{ type: "split", sectionKey: "summary", summaryKey: "summary", subject: "player", bucketKey: "presence_state", splitLabelOverride: "On/Off", primaryDetailTitle: "On/Off Detail", summaryDetailTitle: null }` | `on_off_split` | `summary` | None currently. | On/Off Split, player subject. | One row per `presence_state` bucket (`on`, `off`) in `summary`. | Common columns: `Split`, `GP`, `ORtg`, `DRtg`, `Net`, `Pace`, `MIN`, `+/-` when present. Shows `On/Off Detail`; edge chips for two on/off rows; no row cap/footer/highlight. Current behavior -- needs product decision: backend section is `summary`, not `split_comparison`. | "Jokic on off"; "Nuggets with Jokic on the floor" |
| `player_streak_finder` | `{ type: "streak", sectionKey: "streak" }` | `streak_table` | `streak` | None currently. | Streak Table, player subject. | Ranked player streak row. | `#`, optional `Player`, `Streak`, `Length`, conditional `Status`, `Start`, `End`, `Games`, optional `Record`, available average stat columns. `Status` appears for active/mixed status rows. Detail toggle appears only for undisplayed fields. Backend default limit 25; no frontend cap/footer/highlight. | "Curry longest 20 point streak"; "Giannis triple double streak" |
| `team_streak_finder` | `{ type: "streak", sectionKey: "streak" }` | `streak_table` | `streak` | None currently. | Streak Table, team subject. | Ranked team streak row. | Same Streak Table behavior, with `Team` column unless a single team is pinned. | "Warriors longest winning streak"; "Celtics losing streak" |
| `player_compare` | `{ type: "comparison", subject: "player", headToHead: metadata.head_to_head_used === true }` | `comparison` | `summary`, `comparison` | Any other populated sections render detail toggles. | Comparison Metrics, player subject. | `summary`: one row per player. `comparison`: one row per metric. | Subject cards plus `Metric`, compared-player value columns, `Edge / Difference`. Shows preferred metrics first, then `Show more metrics` for secondary metrics; no footer. Detail toggles for extra summary/comparison fields. | "Jokic vs Embiid"; "Tatum vs Brown last 10 games" |
| `team_compare` | `{ type: "comparison", subject: "team", headToHead: metadata.head_to_head_used === true }` | `comparison` | `summary`, `comparison` | Any other populated sections render detail toggles. | Comparison Metrics, team subject. | `summary`: one row per team. `comparison`: one row per metric. | Same Comparison Metrics behavior with team identities. | "Heat vs Nets"; "Lakers vs Celtics this season" |
| `team_matchup_record` | `{ type: "comparison", subject: "team", headToHead: true }` | `comparison` | `summary`, `comparison` | None currently. | Comparison Metrics, team head-to-head mode. | `summary`: two participant record rows. `comparison`: metric rows, including derived `record` display when available. | Same comparison table, but hero/summary labels use head-to-head language and detail titles become `Participant Detail` / `Metric Detail`. | "Lakers vs Celtics head to head"; "Dubs record vs Lakers" |
| `playoff_history` | `{ type: "playoff_history", mode: "history" }` | `playoff_history` | `summary`, `by_season` | None currently. | Playoff History, history mode. | `summary`: one team postseason aggregate row. `by_season`: season-level playoff rows. | `Season`, `Round Reached`, `Record`, optional `Result`, optional `Opponent`, optional `Win Pct`, optional `Games`. Detail toggles for extra `summary`/`by_season` fields; no cap/footer/highlight. | "Lakers playoff history"; "Spurs playoff history since 2000" |
| `playoff_appearances` | `{ type: "leaderboard", sectionKey: "leaderboard", metricKey: "appearances", sentenceMetricLabel: "playoff appearances" }` | `leaderboard_table` | `leaderboard` for the ranked route contract. | None for ranked mode. | Leaderboard, playoff appearances metric. | Ranked team appearance row. | `#`, `Team`, `Appearances`, then supporting columns such as `Round`, `Seasons`, or `Season` when present. Primary metric highlighted; backend limit applies; no footer/detail. Current behavior -- needs product decision: the backend builder can return a single-team `SummaryResult` when called with `team`, but the frontend route mapping is leaderboard-only. | "Most playoff appearances"; "Teams with most Finals appearances" |
| `playoff_matchup_history` | `{ type: "playoff_history", mode: "matchup" }` | `playoff_matchup_history` | `summary`, `comparison` | None currently. | Playoff History, matchup mode. | `summary`: two team aggregate rows. `comparison`: series/season rows. | `Season`, `Round`, `Winner`, `Series Result`, one record column per team abbreviation, optional `Games`. Detail toggles for extra summary/comparison fields; no cap/footer/highlight. | "Celtics vs Lakers playoffs"; "Heat vs Knicks playoff history" |
| `playoff_round_record` | `{ type: "playoff_history", mode: "round_record" }` | `playoff_round_record` | `leaderboard` | `summary` or `by_season` can be used as fallbacks by the renderer if `leaderboard` is absent. | Playoff History, round-record mode. | Ranked team playoff round record row. | `#`, `Team`, `Round`, `Record`, optional selected metric (`Wins`), `Games`, `Win Pct`, `Seasons`, optional `Series`. Selected metric highlighted; backend limit applies; no footer. | "Best Finals record since 1980"; "Most conference finals wins" |
| `record_by_decade` | `{ type: "record", mode: "record_by_decade" }` | `record_by_decade` | `summary`, `by_season` | None currently. | Record By Decade. | `summary`: one team aggregate row. `by_season`: decade rows, despite the section name. | `Decade`, optional `Seasons`, `W-L`, `Win %`, optional `Games`, optional `Season Type`. `Record Detail` renders from `summary`; `Win %` highlighted; no cap/footer. Current behavior -- needs product decision: decade rows live in `by_season` for backward compatibility. | "Spurs record by decade"; "Bulls playoff record by decade" |
| `record_by_decade_leaderboard` | `{ type: "record", mode: "record_by_decade_leaderboard" }` | `record_by_decade_leaderboard` | `leaderboard` | None currently. | Record By Decade Leaderboard. | Ranked team-decade row. | `#`, `Team`, optional `Decade`, selected metric (`Wins`, `Losses`, `Win %`, `Games`), `W-L`, optional `Games`, optional `Seasons`, optional `Season Type`. Selected metric highlighted; backend limit applies; no footer/detail. | "Best 2010s records"; "Most wins by decade since 1980" |
| `matchup_by_decade` | `{ type: "record", mode: "matchup_by_decade" }` | `matchup_by_decade` | `summary`, `comparison` | None currently. | Matchup By Decade. | `summary`: two overall team rows. `comparison`: one row per decade. | `Decade`, `Games`, one `<team> W-L` column per prefix, optional `<team> Win %`, optional `<team> PPG`. `Matchup Summary Detail` renders from `summary`; no cap/footer/highlight. | "Celtics vs Lakers by decade"; "Knicks vs Heat by decade" |
| `lineup_summary` | `{ type: "entity_summary", sectionKey: "summary" }` | `entity_summary` | `summary` | None currently. | Lineup Summary via Entity Summary. | One trusted lineup aggregate row. | Hero sentence uses `lineup`, `lineup_members`, or `members`, and surfaces `net_rating`, `off_rating`, `def_rating`, and `pace` when present. No visible table, detail drawer, row cap, footer, or highlight. Current behavior -- needs product decision if lineup summaries need a dedicated table/card pattern. | "Tatum and Brown together"; "Luka Kyrie on court stats" |
| `lineup_leaderboard` | `{ type: "leaderboard", sectionKey: "leaderboard", metricKey: "net_rating" }` | `leaderboard_table` | `leaderboard` | None currently. | Leaderboard, lineup metric. | Ranked lineup aggregate row. | `#`, `Name`, `Net`, then inferred supporting columns such as `TM`, `Minutes`, `ORtg`, `DRtg`, `Pace`, `TS%`. Primary metric highlighted; backend limit applies; no footer/detail. Current behavior -- needs product decision: supporting columns are inferred, not route-declared. | "Best 5-man lineups"; "Best Luka Kyrie lineups" |

## Current Weak Contracts

- Leaderboard supporting columns are inferred from row keys and display-order rules. This is current behavior -- needs product decision if per-route columns should become explicit.
- Game-log product row cap is frontend-level (`12` rows) while backend finder limits are route-specific. This is current behavior -- needs product decision if pagination or row caps should change.
- Detail drawers are opt-in by pattern config and only appear when undisplayed row fields exist. This is current behavior -- needs product decision if detail drawers should remain.
- `fallback_table` remains available for unmatched routes, but Wave 1 routes are explicitly guarded from fallback use.
- Wave 2 routes are explicitly guarded from fallback use. `lineup_summary` required a narrow production mapping to leave fallback behavior.
- `player_on_off` currently renders a split pattern over `SummaryResult.summary` rows keyed by `presence_state`; it does not use the `SplitSummaryResult`/`split_comparison` section convention.
- `record_by_decade` stores decade rows in the `by_season` section for backward compatibility.
- `playoff_appearances` is locked as a ranked leaderboard route for the UI. Direct backend calls with `team` can return a `SummaryResult`; this needs a product decision before that variant is advertised in UI contracts.
- `lineup_summary` now renders through the generic entity-summary pattern with lineup-aware hero text, but it does not yet have a dedicated lineup table or illustration.
- `routeToPattern` remains a route-specific switch statement. This wave intentionally did not convert it to a registry.
