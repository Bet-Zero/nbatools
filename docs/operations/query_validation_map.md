# Query Validation Map

## Purpose

NBA Tools has several query-validation layers. They answer different
questions and must not be cited as if they were interchangeable.

The Raw QA harness is implemented by `tools/raw_query_answer_qa.py`. Its
curated case source is `qa/raw_query_answer_corpus.yaml`. Saved subset
selectors live under `qa/harness_slices/`. Python query tests are configured
separately through `Makefile` and `pyproject.toml`.

## Validation Layers

### Full Raw QA Corpus

- Source: `qa/raw_query_answer_corpus.yaml`
- Command:

  ```bash
  .venv/bin/python tools/raw_query_answer_qa.py \
    --corpus qa/raw_query_answer_corpus.yaml \
    --fail-on-expectation-failure
  ```

- Meaning: runs all corpus cases from the YAML.
- Important: this is the full corpus, not a slice and not pytest.

### Raw QA Corpus Slice

- Source: `qa/harness_slices/<slice>.yaml`, selecting case IDs from the full
  corpus.
- Command:

  ```bash
  .venv/bin/python tools/raw_query_answer_qa.py \
    --corpus qa/raw_query_answer_corpus.yaml \
    --slice <slice> \
    --fail-on-expectation-failure
  ```

- Meaning: runs a named subset of the full corpus.
- Important: a slice is not a separate corpus and a slice result is not a
  full-corpus result.

### Product Review Doc

- Output: `outputs/raw_query_answer_qa/<run_id>/product_review.md`
- Meaning: human-readable review artifact generated by the Raw QA harness.
- Important: this is not a separate corpus and not a separate test source.

The product review doc helps a reviewer inspect family coverage,
representative answers, suspicious rows, and product decisions. It does not
change the scope of the harness run that generated it.

### Rendered UI Review

- Output: `outputs/public_ui_render_review/<run_id>/`
- Meaning: browser-rendered screenshot/text review for selected representative
  queries.
- Important: this checks actual UI presentation, not the whole corpus unless
  explicitly run that way.

### Pytest Query Tests

- Command: `make test-query`
- Expands to: `pytest -m query`
- Meaning: Python tests marked `query`.
- Important: this is separate from the Raw QA corpus and should not be cited
  as corpus evidence.

## Current Scoreboard

Snapshot: 2026-06-06.

### Corpus And Slices

| Scope | Source | Current case count |
| --- | --- | ---: |
| Full Raw QA corpus | `qa/raw_query_answer_corpus.yaml` | 301 |
| `basic_public_availability` | `qa/harness_slices/basic_public_availability.yaml` | 7 |
| `defensive_aliases` | `qa/harness_slices/defensive_aliases.yaml` | 8 |
| `natural_query_route_priority` | `qa/harness_slices/natural_query_route_priority.yaml` | 41 |
| `player_entity_stat_context` | `qa/harness_slices/player_entity_stat_context.yaml` | 9 |
| `playoff_phrasing` | `qa/harness_slices/playoff_phrasing.yaml` | 12 |
| `product_boundaries` | `qa/harness_slices/product_boundaries.yaml` | 20 |
| `public_query_acceptance` | `qa/harness_slices/public_query_acceptance.yaml` | 114 |
| `team_date_context` | `qa/harness_slices/team_date_context.yaml` | 9 |

### Current Evidence

| Artifact | Evidence pointer | Scope note |
| --- | --- | --- |
| Full Raw QA corpus run | `outputs/raw_query_answer_qa/20260606T233148Z/` | Current 301-case corpus run. All 301 expectation cases passed. |
| `public_query_acceptance` slice run | `outputs/raw_query_answer_qa/20260606T232744Z/` | Current 114-case `public_query_acceptance` slice run. All 114 expectation cases passed. |
| Latest local public-acceptance review path | `outputs/raw_query_answer_qa/latest_public_query_acceptance/` | Mutable latest-mode folder for repeated local review. Do not cite it as durable product evidence. |
| Current generated `product_review.md` | `outputs/raw_query_answer_qa/20260606T232744Z/product_review.md` | Generated by the current 114-case `public_query_acceptance` slice run. A product-review artifact retains the scope of its generating run. |
| Rendered UI review | Durable summary below | Selected representative browser-rendered queries passed; the older generated screenshot folder is not retained as durable source of truth. |

Latest full Raw QA corpus run result:

| Metric | Result |
| --- | ---: |
| Cases | 301 |
| Expectation cases passed | 301 |
| Failed case IDs | none |
| Result status: `ok` | 230 |
| Result status: `no_result` | 50 |
| Result status: `error` | 21 |
| Suspicious flag cases | 0 |
| Informational flag cases | 174 |
| Verified outlier cases | 1 |

Latest `public_query_acceptance` slice run result:

| Metric | Result |
| --- | ---: |
| Cases | 114 |
| Expectation cases passed | 114 |
| Failed case IDs | none |
| Result status: `ok` | 70 |
| Result status: `no_result` | 25 |
| Result status: `error` | 19 |
| Suspicious flag cases | 0 |
| Informational flag cases | 47 |
| Verified outlier cases | 1 |
| Human product review | `human_review_complete` for the tracked `public_query_acceptance` closure in `qa/raw_query_answer_acceptance_families.yaml` |
| UI spot checks | Not applicable for the opponent-division promotion; the change reuses the existing `team_record` renderer and introduced no frontend layout change |

The current human product-review state is complete for the public-acceptance
closure tracked in `qa/raw_query_answer_acceptance_families.yaml`. The generated
outputs remain evidence snapshots only; durable status is recorded in this map and
[`raw_product_release_status.md`](../reference/raw_product_release_status.md).

The Raw QA `summary.json` artifact records the corpus path and number of cases
run, but it does not currently record the requested slice name. For unnamed
run directories, cite a slice scope only after matching the run's
`report.jsonl` case IDs to the saved slice manifest. Prefer explicit run IDs
for future human-facing evidence when practical.

## How To Talk About Results

Use these terms precisely:

- `machine-passing`: Raw QA expectations passed for the stated corpus scope:
  full corpus, slice, or single case.
- `product-reviewed`: a human/ChatGPT reviewed the generated
  `product_review.md` or equivalent answer artifact.
- `UI-reviewed`: rendered browser output/screenshots/text snapshots were
  reviewed.
- `deferred polish`: known issue, not blocking the current validation scope.

Reporting rules:

- Do not say "the corpus passed" unless the full unsliced Raw QA corpus was
  run.
- Say "the `public_query_acceptance` slice passed" when only that slice was
  run.
- Do not mix pytest query counts with corpus counts.
- Do not treat return packages as durable source-of-truth docs.

## Source Inventory

Use these durable sources when validating or refreshing this map:

| Source | Purpose |
| --- | --- |
| `Makefile` | `test-query` and Raw QA convenience targets |
| `pyproject.toml` | pytest marker configuration |
| `tools/raw_query_answer_qa.py` | Raw QA harness behavior and generated paths |
| `qa/raw_query_answer_corpus.yaml` | full curated Raw QA corpus |
| `qa/harness_slices/` | named Raw QA subset selectors |
| `outputs/raw_query_answer_qa/` | generated Raw QA reports and product-review artifacts |
| `outputs/public_ui_render_review/` | generated rendered UI review artifacts |

Return packages may serve as historical receipts for a particular handoff.
They are not authoritative validation docs and are not long-term evidence for
the current scoreboard.
